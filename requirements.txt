# No Python dependencies: waypoints.py uses only the standard library.
# The geometry work is done by pfaedle, which runs from its official Docker image
# (ghcr.io/ad-freiburg/pfaedle) — Docker is preinstalled on GitHub's ubuntu runners.
